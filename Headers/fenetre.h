#ifndef FENETRE_H
#define FENETRE_H

#include<QDialog>
#include"jouer.h"

/*class jouer;

class fenetre
{
public:
    fenetre(QWidget *fenetre);
    ~fenetre();
    void tracerTable(QWidget *fenetre);
    bool isThereMoreMoves();

private:
    QVector< QVector< QBrainH* > > icone;
    jouer *table;
    QVector< QVector< BrainCrush* > > Board;
};
*/

#endif // FENETRE_H
