#include "fenetre.h"
#include"Headers.h"
/*
fenetre::fenetre(QWidget *fenetre)
{
    table=new jouer;
    Board.resize(10);
    for(int i=0;i<10;i++){
        Board[i].resize(10);
    }
    for(int i=0;i<10;i++){
            for(int j=0;j<10;j++)
            {
                Board[i][j]=NULL;
            }
    }
    table->reset(10);
}
fenetre::~fenetre(){}

void fenetre::tracerTable(QWidget *fenetre){
    int x=400;
    for(int i = 0; i < 10; i++)
     {

         int y=0;//lignes
        for(int j=0;j<10;j++){




            if (icone[i][j]->getCouleur()==4){
                icone[i][j]->setPixmap(QPixmap(icone[i][j]->getType()));
                icone[i][j]->setGeometry(x, y, 80, 80);

            }
            else if (icone[i][j]->getCouleur()==1){
                icone[i][j]->setPixmap(QPixmap(icone[i][j]->getType()));
                icone[i][j]->setGeometry(x, y, 80, 80);
            }
            else if (icone[i][j]->getCouleur()==2){
                icone[i][j]->setPixmap(QPixmap(icone[i][j]->getType()));
                icone[i][j]->setGeometry(x, y, 80, 80);
            }
            else if (icone[i][j]->getCouleur()==3){
                icone[i][j]->setPixmap(QPixmap(icone[i][j]->getType()));
                icone[i][j]->setGeometry(x, y, 80, 80);
            }
            else if (icone[i][j]->getCouleur()==5){
                icone[i][j]->setPixmap(QPixmap(icone[i][j]->getType()));
                icone[i][j]->setGeometry(x, y, 80, 80);
            }
            y+=80;
        }
        x+=80;
    }

}
bool fenetre::isThereMoreMoves(){

}

*/
